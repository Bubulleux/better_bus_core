name: better_bus_core
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.6.0

resolution: workspace

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  http: ^1.2.2
  latlong2: ^0.9.1
  path: ^1.9.0
  archive: ^3.6.1
  intl: ^0.20.2

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
  test_descriptor: ^2.0.2
